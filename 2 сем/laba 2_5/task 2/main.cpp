#include "pack.h"
#include "prototypes.h"


int main() {

    setlocale(0, "");
    srand(time(0));

    vec main_vector{};
    arr sort_array{};

    main_function(main_vector,sort_array);

    return 0;
}